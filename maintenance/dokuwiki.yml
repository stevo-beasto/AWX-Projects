---

  - name: Maintenance DokuWiki
    hosts: DOCKERHOST-PROD
    become: yes
    become_user: root
    vars:
      - demo: "{{ lookup('hashi_vault', 'secret=home-environment-live/beast/system-password token=s.ztKWCLh4NEvzvNMdvEzO1G6U url=https://beast.foxy.home:8200 validate_certs=false') }}"
    tasks:

    - name: Run the rebuild script for DOKUWIKI
      shell:
         "/Users/hank/Docker/wikis/music/bin/rebuild-music_wiki.sh"
      args: 
         executable: /bin/bash
