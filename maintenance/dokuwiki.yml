---

  - name: Maintenance DokuWiki
    hosts: DOCKERHOST-PROD
    tasks:

    - name: Run the rebuild script for DOKUWIKI
      shell:
         " rebuild-music_wiki "
      register: maintenance-dokuwiki
      tags: maintenance-dokuwiki

- debug: msg="{{maintenance-dokuwiki.stdout}}"
